#!/usr/bin/env kiwi
/* Test script for conditional statements */

void summa(string $message, int $a, int $b) {
    print $message; print ": ";
    if ($a < $b) {
        print "ON";
    } else {
        print "OFF";
    }
    print "\n";
}

void main() {
    auto $a = 10;
    auto $b = 12;

    // summa("A < B", $a, $b);
    // summa("B < A", $a, $b);
}
