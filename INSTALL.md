INSTALL
=======

Linux
-------
Download from [LLVM.org](http://llvm.org/) LLVM 3.0 and build and install;

    ./configure
    make
    sudo make install

Install ICU, Boost

Build and install Kiwi:

    ./configure
    make
    sudo make install
